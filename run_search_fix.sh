#!/bin/bash

# ═══════════════════════════════════════════════════════════════
# سكريبت تشغيل واجهة البحث المحدثة - كوريزا OSINT
# ═══════════════════════════════════════════════════════════════

echo "╔══════════════════════════════════════════════════════════════╗"
echo "║         🚀 تشغيل واجهة البحث المحدثة - كوريزا             ║"
echo "╚══════════════════════════════════════════════════════════════╝"
echo ""

# التحقق من وجود Python
if ! command -v python3 &> /dev/null; then
    echo "❌ خطأ: Python 3 غير مثبت"
    exit 1
fi

echo "✅ Python 3 موجود"
echo ""

# الخطوة 1: إضافة الأدوات الجديدة
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📦 الخطوة 1: إضافة الأدوات الجديدة"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
python3 manage.py add_web_sources

if [ $? -eq 0 ]; then
    echo "✅ تم إضافة الأدوات بنجاح"
else
    echo "⚠️  تحذير: قد تكون الأدوات مضافة مسبقاً"
fi
echo ""

# الخطوة 2: التحقق من الملفات
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔍 الخطوة 2: التحقق من الملفات"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

if [ -f "templates/osint_tools/analytics.html" ]; then
    echo "✅ analytics.html موجود"
else
    echo "❌ analytics.html مفقود"
fi

if [ -f "templates/osint_tools/simple_search_interface.html" ]; then
    echo "✅ simple_search_interface.html موجود"
else
    echo "❌ simple_search_interface.html مفقود"
fi
echo ""

# الخطوة 3: عرض معلومات التشغيل
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🌐 الخطوة 3: تشغيل الخادم"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "سيتم تشغيل الخادم على:"
echo "  🔗 http://127.0.0.1:8000"
echo ""
echo "الصفحات المتاحة:"
echo "  📄 واجهة البحث: http://127.0.0.1:8000/osint/search/"
echo "  📊 التحليلات: http://127.0.0.1:8000/osint/analytics/"
echo "  🏠 لوحة التحكم: http://127.0.0.1:8000/osint/"
echo ""
echo "للإيقاف: اضغط Ctrl+C"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# تشغيل الخادم
python3 manage.py runserver
